# netra-privacy

The privacy policy for **Netra**, published via GitHub Pages so the App Store
and Play Console have a stable public URL to fetch.

This repository is public because store review requires a reachable policy URL.
It contains the policy and nothing else — the app source lives in a private
repository.

Source of truth: `docs/PRIVACY.md` in the app repo. Edit it there, copy here.
